<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="rumos_client.Pages.FunctionSelectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:rumos_client.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!--フェードアウト設定-->
        <Storyboard x:Name="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetName="BackgroundImage"
                             Storyboard.TargetProperty="Opacity"
                             To="0"
                             Duration="0:0:0.2"/>
        </Storyboard>
        <!--フェードイン設定-->
        <Storyboard x:Name="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetName="BackgroundImage"
                             Storyboard.TargetProperty="Opacity"
                             To="0.8"
                             Duration="0:0:0.2"/>
        </Storyboard>
    </Page.Resources>

    <Grid x:Name="RootGrid" Tapped="RootGrid_Tapped" SizeChanged="RootGrid_SizeChanged">
        <Image x:Name="BackgroundImage"
               Stretch="UniformToFill"
               Opacity="0.8"
               VerticalAlignment="Center"
               HorizontalAlignment="Center"
               Canvas.ZIndex="0"
               
              />
        <Border CornerRadius="10" 
                BorderBrush="White"
                BorderThickness="2"
                Width="130"
                Height="100"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="100"
                Canvas.ZIndex="2">
            <Button HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Click="ToList">
                <Image Source="/Assets/Images/menu_bar.png" />
            </Button>
            
        </Border>
        <StackPanel x:Name="SelectionMenuPanel" Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="200,0" Canvas.ZIndex="2">
            <TextBlock Text="・I Control" Style="{StaticResource SelectionMenuLabel}" Foreground="#9B77FF" />
            <TextBlock Text="・Magic Routine" Style="{StaticResource SelectionMenuLabel}" />
            <TextBlock Text="・Device" Style="{StaticResource SelectionMenuLabel}" />
            <TextBlock Text="・Environment" Style="{StaticResource SelectionMenuLabel}" />
            <TextBlock Text="・Setting" Style="{StaticResource SelectionMenuLabel}"/>
        </StackPanel>
        <ScrollViewer x:Name="Scroller"
                      VerticalScrollBarVisibility="Hidden"
                      VerticalScrollMode="Enabled"
                      HorizontalScrollMode="Disabled"
                      ZoomMode="Disabled"
                      ViewChanged="Scroller_ViewChanged"
                      Canvas.ZIndex="1">
            <StackPanel x:Name="PanelStack">
                <Grid  Height="{Binding ActualHeight,ElementName=RootGrid}" Width="{Binding ActualWidth,ElementName=RootGrid}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock Text="I Control" VerticalAlignment="Center" FontSize="100" />
                        <Border Margin="20,0" Width="100" Height="100" BorderBrush="White" BorderThickness="2" CornerRadius="10">
                            <Image Source="/Assets/Images/icontrol_icon.png" />
                        </Border>
                    </StackPanel>
                </Grid>
                <Grid  Height="{Binding ActualHeight,ElementName=RootGrid}" Width="{Binding ActualWidth,ElementName=RootGrid}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock Text="MagicRoutine" VerticalAlignment="Center" FontSize="100" />
                        <Border Margin="20,0" Width="100" Height="100" BorderBrush="White" BorderThickness="2" CornerRadius="10">
                            <Image Source="/Assets/Images/magicroutin_icon.png" />
                        </Border>
                    </StackPanel>
                </Grid>
                <Grid  Height="{Binding ActualHeight,ElementName=RootGrid}" Width="{Binding ActualWidth,ElementName=RootGrid}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock Text="Device" VerticalAlignment="Center" FontSize="100"/>
                        <Border Margin="20,0" Width="100" Height="100" BorderBrush="White" BorderThickness="2" CornerRadius="10">
                            <Image Source="/Assets/Images/Device_icon.png" />
                        </Border>
                    </StackPanel>
                </Grid>
                <Grid  Height="{Binding ActualHeight,ElementName=RootGrid}" Width="{Binding ActualWidth,ElementName=RootGrid}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock Text="Environment" VerticalAlignment="Center" FontSize="100"/>
                        <Border Margin="20,0" Width="100" Height="100" BorderBrush="White" BorderThickness="2" CornerRadius="10">
                            <Image Source="/Assets/Images/Environment_icon.png" />
                        </Border>
                    </StackPanel>
                </Grid>
                <Grid  Height="{Binding ActualHeight,ElementName=RootGrid}" Width="{Binding ActualWidth,ElementName=RootGrid}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock Text="Setting" VerticalAlignment="Center" FontSize="100"/>
                        <Border Margin="20,0" Width="100" Height="100" BorderBrush="White" BorderThickness="2" CornerRadius="10">
                            <Image Source="/Assets/Images/setting_icon.png" />
                        </Border>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>

</Page>
