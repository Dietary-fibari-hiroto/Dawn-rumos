<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="rumos_client.Pages.FunctionSelectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:rumos_client.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <!-- 背景画像 2枚重ね -->
        <Image x:Name="Background1" Source="/Assets/Images/IMG_7028.jpg" Stretch="UniformToFill" />
        <Image x:Name="Background2" Source="/Assets/Images/IMG_7053.jpg" Stretch="UniformToFill" Opacity="0"/>

        <!-- 文字 -->
        <TextBlock x:Name="TextCurrent"
               Text="最初の文字"
               FontSize="48"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               RenderTransformOrigin="0.5,0.5">
            <TextBlock.RenderTransform>
                <TranslateTransform Y="0"/>
            </TextBlock.RenderTransform>
        </TextBlock>

        <TextBlock x:Name="TextNext"
               Text="次の文字"
               FontSize="48"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Opacity="0"
               RenderTransformOrigin="0.5,0.5">
            <TextBlock.RenderTransform>
                <TranslateTransform Y="50"/>
                <!-- 下から入る -->
            </TextBlock.RenderTransform>
        </TextBlock>

        <!-- ボタンで切り替え -->
        <Button Content="Next" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="Next_Click"/>
    </Grid>

</Page>
