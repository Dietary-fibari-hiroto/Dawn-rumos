<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="rumos_client.Pages.FunctionListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:rumos_client.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:components="using:rumos_client.Components"
    mc:Ignorable="d">

    <Grid>
        <Border CornerRadius="10" 
                BorderBrush="White"
                BorderThickness="2"
                Width="130"
                Height="100"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="100"
                Canvas.ZIndex="2">
            <Button HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Click="ToSelector"
                  >
                <Image Source="/Assets/Images/menu_bar.png" />
            </Button>
        </Border>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center" x:Name="CardGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        </Grid>
        
        
    </Grid>
</Page>
