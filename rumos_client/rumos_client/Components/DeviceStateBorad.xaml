<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="rumos_client.Components.DeviceStateBorad"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:rumos_client.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border Width="1000"  Height="700" BorderBrush="White" BorderThickness="1" Padding="50">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- 左側の情報パネル -->
            <StackPanel Grid.Column="0" Orientation="Vertical" Spacing="20" HorizontalAlignment="Left">
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Id" Foreground="#9B77FF" FontSize="15" />
                    <TextBlock x:Name="DeviceIdLabel" FontSize="20" Margin="20,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Name" Foreground="#9B77FF" FontSize="15" />
                    <TextBlock x:Name="DeviceNameLabel" FontSize="20" Margin="20,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Category" Foreground="#9B77FF" FontSize="15" />
                    <TextBlock x:Name="DeviceCategoryLabel" FontSize="20" Margin="20,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Ip" Foreground="#9B77FF" FontSize="15" />
                    <TextBlock  x:Name="DeviceIpLabel" FontSize="20" Margin="20,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Connect" Foreground="#9B77FF" FontSize="15" />
                    <Border x:Name="ConnectionBar" HorizontalAlignment="Left" Background="Gray" BorderThickness="1" BorderBrush="White" Width="150" Height="20" Margin="20,0,0,0"/>
                </StackPanel>
                <Button VerticalAlignment="Bottom" HorizontalAlignment="Right" Click="PowerSupply">
                    <Border  Width="150" Height="150" CornerRadius="75" BorderBrush="Wheat" BorderThickness="1">
                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="Supply" FontSize="20"/>
                    </Border>
                </Button>
            </StackPanel>

            <!-- 右側のカラーピッカー＆ボタン -->
            <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Stretch">
                <ColorPicker 
             x:Name="colorPicker"
                     ColorChanged="ColorPicker_ColorChanged"
             IsAlphaEnabled="False"
             IsColorPreviewVisible="True"
             HorizontalAlignment="Right"
             VerticalAlignment="Top"
         />

            </StackPanel>
        </Grid>

    </Border>
</UserControl>
