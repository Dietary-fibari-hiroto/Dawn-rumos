<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="rumos_client.Tests.TestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:rumos_client.Tests"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <!-- フェードアウト -->
        <Storyboard x:Name="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetName="BackgroundImage"
                         Storyboard.TargetProperty="Opacity"
                         To="0"
                         Duration="0:0:0.2"/>
        </Storyboard>

        <!-- フェードイン -->
        <Storyboard x:Name="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetName="BackgroundImage"
                         Storyboard.TargetProperty="Opacity"
                         To="1"
                         Duration="0:0:0.2"/>
        </Storyboard>
    </Page.Resources>


    <Grid x:Name="RootGrid" Background="Black" Tapped="RootGrid_Tapped" SizeChanged="RootGrid_SizeChanged">
        <Image x:Name="BackgroundImage"
           Stretch="UniformToFill"
           Opacity="1"
           VerticalAlignment="Center"
           HorizontalAlignment="Center"/>
        <ScrollViewer x:Name="Scroller"
                      VerticalScrollBarVisibility="Hidden"
                      VerticalScrollMode="Enabled"
                      HorizontalScrollMode="Disabled"
                      ZoomMode="Disabled"
                      ViewChanged="Scroller_ViewChanged">

            <StackPanel x:Name="PanelStack">
                <Border Background="Transparent" Height="{Binding ActualHeight, ElementName=RootGrid}">
                    <TextBlock Text="Section 1" FontSize="48" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border Background="Transparent" Height="{Binding ActualHeight, ElementName=RootGrid}">
                    <TextBlock Text="Section 2" FontSize="48" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border Background="Transparent" Height="{Binding ActualHeight, ElementName=RootGrid}">
                    <TextBlock Text="Section 3" FontSize="48" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>

</Page>
